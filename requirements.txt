# TCFD repo dependencies — the scripts/ toolchain, installed in .venv (Python 3.9.6).
# (Until 2026-08-21 the archived isimip-pipeline package's pyproject doubled as the
# de-facto manifest; this file replaces that role.)
numpy
scipy
pandas
xarray
netCDF4
plotly
PyYAML
shapely
rasterio       # scripts/process_landslide_arup.py — reads the Arup GeoTIFF (function-local import)
py7zr          # scripts/measure_hail_nature2026.py — reads the 7z hail deposit archive
pytest         # scripts/tests/ (run `pytest` from the repo root; see pytest.ini)

# --- Water Risk Index download path — NOT installed in this venv ---
# Required only by scripts/download_water_*.py via the vendored
# scripts/utils/isimip_api/ client. Install before any water re-ingest:
# httpx
# rich
# isimip-client

# Deliberately NOT installed: geopandas — only scripts/utils/natural_earth.py imports it
# (module-level), and that module is documented as unusable in this venv.
