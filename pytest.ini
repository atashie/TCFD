[pytest]
# Only scripts/tests/ holds pytest-collected tests. The four scripts/test_*.py files at
# the top of scripts/ are CLI verifiers run as programs (they exit non-zero on violation)
# and must never be collected by pytest — this testpaths scoping guarantees that.
testpaths = scripts/tests
